[include]
    path = ~/.config/git/self-config
[core]
    editor = nvim
    excludesfile = ~/.config/git/ignore
    whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
    pager = delta
[color]
    ui = auto
[color "branch"]
    current = yellow bold
    local = green bold
    remote = cyan bold
[color "status"]
    added = green
    changed = yellow
    untracked = red
[alias]
    co = checkout
    up = pull --rebase --autostash
    lp = log --pretty='%C(red)%h%Creset%C(yellow)%d%Creset %s %C(cyan)(%ar)%Creset'
    new = lp master..HEAD
    missing = lp HEAD..master
[push]
    default = simple
[pull]
    rebase = true
[merge]
    conflictstyle = diff3
    tool = nvim -d
[rebase]
    autosquash = true
    autoStash = true
[credential]
    helper = osxkeychain
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)       pager = delta
[diff]
    colorMoved = default
